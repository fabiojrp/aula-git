# Login
gh auth logout -h github.com
gh auth status
gh auth login -h github.com


# Criar repositório
1 - git init (cria repositório local) ou git clone
2 - git add . (seleciona todo os arquivos p serem enviados para commit)
3 - git commit -m "mensagem"
4 - git remote add origin url-http
5 - git push -u origin main (só na 1a vez)
6 - git push (nas demais vezes)
7 - git pull pra download

echo "# fundamentosjs" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/fabiojrp/helloworld.git
git push -u origin main (só na 1a vez)
git push (nas demais vezes)